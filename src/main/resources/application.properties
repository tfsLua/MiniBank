# Altera a estrutura da tabela caso haja mudanças na entidade.
spring.jpa.hibernate.ddl-auto=update

# Configurações do hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Acessa ao banco de dados  
spring.datasource.url=jdbc:mysql://${MYSQL_host: localhost}:3306/pitangdb

# Usuário do banco de dados
spring.datasource.username=root

# Senha do bancco de dados
spring.datasource.password=casa123